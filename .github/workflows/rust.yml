on: push

name: CI

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/cache@preview
        id: cache
        with:
          path: target/debug
          key: ${{ runner.os }}-${{ hashFiles('**/Cargo.lock')}}
      - uses: actions-rs/cargo@v1
        with:
          command: test

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/cache@preview
        id: cache
        with:
          path: target/release
          key: ${{ runner.os }}-${{ hashFiles('**/Cargo.lock')}}
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release --all-features

  build_docker_image:
    name: Build docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build and publish docker image
        run: GITHUB_TOKEN=foo ./bin/publish
        #run: GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }} ./bin/publish

  #publish_deployment_artifact:
    #name: Publish deployment artifact
    #runs-on: ubuntu-latest
    #steps:
      #- uses: actions/checkout@v1
      #- uses: actions/cache@preview
        #id: cache
        #with:
          #path: target/release
          #key: ${{ runner.os }}-${{ hashFiles('**/Cargo.lock')}}
      #- name: Build and publish docker image
        #run: docker build -t ${GITHUB_REPOSITORY}:${GITHUB_SHA} .
